require "option_parser"

parser = OptionParser.new do |parser|
  parser.banner = "usage: <%= @appname %>"

  parser.on("-v", "--version", "dispaly the version") { puts <%= @apptitle %>::VERSION; exit 0 }
  parser.on("-h", "--help", "show this help") { puts parser; exit 0 }
end

parser.parse!
